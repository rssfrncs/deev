[build]
builder = "railpack"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/trpc/video.topTags"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
